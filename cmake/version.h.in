#pragma once

// clang-format off
#define _SVNLA_STR(A) #A
#define SVNLA_STR(A) _SVNLA_STR(A)

#define SVNLA_MAJOR ${PROJECT_VERSION_MAJOR}
#define SVNLA_MINOR ${PROJECT_VERSION_MINOR}
#define SVNLA_PATCH ${PROJECT_VERSION_PATCH}

#define GIT_HASH "${GIT_HASH}"
#define GIT_BRANCH "${GIT_BRANCH}"

#define SVNLA_VERSION (10000 * SVNLA_MAJOR + 100 * SVNLA_MINOR + SVNLA_PATCH)
#define SVNLA_VERSION_STR SVNLA_STR(SVNLA_MAJOR.SVNLA_MINOR.SVNLA_PATCH)
#define SVNLA_VERSION_STR_WITH_GIT SVNLA_VERSION_STR SVNLA_STR(GIT_HASH)

#define SVNLA_BUILD_DATE "${BUILD_DATE}"
#define SVNLA_BUILD_TIME "${BUILD_TIME}"

#define SVNLA_BUILD_STR SVNLA_BUILD_DATE " " SVNLA_BUILD_TIME

// clang-format on
