# Client UI Knowledge

## 模块范围

- 主界面、导航、设置页、插件管理页、下载页面等用户可见交互。

## 已知规则

- 状态切换类 UI 应避免只修改控件显示，必须通过业务层接口写入真实状态。
- 如果操作失败，应回滚 UI 状态并给出反馈。
- 用户可见文本变更需要检查翻译影响。

## 证据

- 需要结合实际任务读取 `sVanilla/src/ClientUi`。
